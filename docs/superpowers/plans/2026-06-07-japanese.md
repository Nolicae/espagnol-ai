# Japanese Language Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add Japanese (日本語) as the 8th language in the espagnol-ai PWA, with 6 edge-tts ja-JP voices, a Vercel proxy, and full wiring in index.html.

**Architecture:** Same consolidated pattern as French and Chinese — voices added to the existing `nolicae-nolicae-tts.hf.space` Docker space (`nolicae-tts/core/voices.py`), a new Vercel serverless proxy at `/api/tts-ja.js`, and all frontend data (LANGUAGES.ja, UI_STRINGS.ja, meta, system, diff, voices, CSS, HTML) added to `espagnol-ai/index.html`. The SVG map is bounded to Japan's main islands; all 6 voices are `ja-JP` locales with ≥41px separation between dots.

**Tech Stack:** edge-tts (Microsoft Neural), HuggingFace Spaces Docker, Vercel Node.js serverless, vanilla JS/HTML/CSS service worker.

---

## File Map

| File | Action | What changes |
|------|--------|-------------|
| `nolicae-tts/core/voices.py` | Modify | Append 6 ja-JP entries to EDGE_VOICES |
| `espagnol-ai/api/tts-ja.js` | Create | Vercel proxy for Japanese TTS |
| `espagnol-ai/index.html` | Modify (×7) | LANGUAGES.ja, UI_STRINGS.ja, meta, system, diff, voices, speakChunk, whisperLang, CSS, HTML buttons |
| `espagnol-ai/service-worker.js` | Modify | Bump cache v63 → v64 |

---

## Voice Reference

All voices are `ja-JP`. Map bounds: `lonMin: 128, lonMax: 148, latMax: 46, latMin: 30, w: 360, h: 296`.

| Edge-TTS ID | Display | City | Country | Trait | Color | x | y |
|---|---|---|---|---|---|---|---|
| `ja-JP-NanamiNeural` (F) | 七海 | 東京 | 日本 | 丁寧 | `#C03060` | 211 | 191 |
| `ja-JP-KeitaNeural` (M) | 圭太 | 大阪 | 日本 | 元気 | `#D06020` | 135 | 209 |
| `ja-JP-AoiNeural` (F) | 葵 | 札幌 | 日本 | 爽やか | `#2050A0` | 241 | 54 |
| `ja-JP-DaichiNeural` (M) | 大智 | 仙台 | 日本 | 温かい | `#305040` | 232 | 143 |
| `ja-JP-ShioriNeural` (F) | 詩織 | 広島 | 日本 | 穏やか | `#702080` | 81 | 215 |
| `ja-JP-NaokiNeural` (M) | 直紀 | 福岡 | 日本 | 明るい | `#208080` | 43 | 230 |

Minimum dot separation: Hiroshima–Fukuoka ≈ 41px. All other pairs ≥ 52px.

---

### Task 1: Add Japanese voices to nolicae-tts + push HF

**Files:**
- Modify: `C:\Users\0deco\Downloads\nolicae-tts\core\voices.py` (lines 143–144)

- [ ] **Step 1: Open voices.py and locate the insertion point**

The file currently ends the EDGE_VOICES list at line 143–144:
```python
    {"languageCodes": ["zh-TW"], "name": "zh-TW-HsiaoChenNeural", "ssmlGender": "FEMALE", "naturalSampleRateHertz": 24000, "backend": "edge"},
]
```

- [ ] **Step 2: Insert 6 Japanese voice entries before the closing `]`**

Replace the final two lines (zh-TW entry + `]`) with:
```python
    {"languageCodes": ["zh-TW"], "name": "zh-TW-HsiaoChenNeural", "ssmlGender": "FEMALE", "naturalSampleRateHertz": 24000, "backend": "edge"},
    # Japanese — 6 voices (ja-JP)
    {"languageCodes": ["ja-JP"], "name": "ja-JP-NanamiNeural",  "ssmlGender": "FEMALE", "naturalSampleRateHertz": 24000, "backend": "edge"},
    {"languageCodes": ["ja-JP"], "name": "ja-JP-KeitaNeural",   "ssmlGender": "MALE",   "naturalSampleRateHertz": 24000, "backend": "edge"},
    {"languageCodes": ["ja-JP"], "name": "ja-JP-AoiNeural",     "ssmlGender": "FEMALE", "naturalSampleRateHertz": 24000, "backend": "edge"},
    {"languageCodes": ["ja-JP"], "name": "ja-JP-DaichiNeural",  "ssmlGender": "MALE",   "naturalSampleRateHertz": 24000, "backend": "edge"},
    {"languageCodes": ["ja-JP"], "name": "ja-JP-ShioriNeural",  "ssmlGender": "FEMALE", "naturalSampleRateHertz": 24000, "backend": "edge"},
    {"languageCodes": ["ja-JP"], "name": "ja-JP-NaokiNeural",   "ssmlGender": "MALE",   "naturalSampleRateHertz": 24000, "backend": "edge"},
]
```

- [ ] **Step 3: Commit and push to HuggingFace**

```bash
cd C:\Users\0deco\Downloads\nolicae-tts
git add core/voices.py
git commit -m "feat: add 6 Japanese (ja-JP) voices"
git push hf master:main
```

Expected: push succeeds. HF Space will rebuild (takes ~2 min). The space URL `https://nolicae-nolicae-tts.hf.space` will wake and serve `ja-JP-NanamiNeural` etc. via `POST /v1/text:synthesize`.

---

### Task 2: Create Vercel proxy tts-ja.js

**Files:**
- Create: `C:\Users\0deco\Downloads\espagnol-ai\api\tts-ja.js`

- [ ] **Step 1: Create the file**

```js
// Vercel serverless function — nolicae-tts consolidated space (Japanese voices)
const TTS_BASE_URL = 'https://nolicae-nolicae-tts.hf.space';
const DEFAULT_VOICE = 'ja-JP-NanamiNeural';
const RETRY_DELAYS = [2000, 4000, 6000]; // retry up to 3x if space is waking

async function japaneseTTS(text, voice = DEFAULT_VOICE) {
  const body = JSON.stringify({
    input: { text },
    voice: { name: voice },
    audioConfig: { audioEncoding: 'MP3' }
  });

  let lastErr;
  for (let attempt = 0; attempt <= RETRY_DELAYS.length; attempt++) {
    if (attempt > 0) {
      await new Promise(r => setTimeout(r, RETRY_DELAYS[attempt - 1]));
    }
    try {
      const response = await fetch(`${TTS_BASE_URL}/v1/text:synthesize`, {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body,
      });
      if (!response.ok) {
        lastErr = new Error(`japanese-tts ${response.status}`);
        continue;
      }
      const data = await response.json();
      const audio = Buffer.from(data.audioContent, 'base64');
      if (audio.length < 100) {
        lastErr = new Error('audio too short, retrying');
        continue;
      }
      return audio;
    } catch (e) {
      lastErr = e;
    }
  }
  throw lastErr;
}

module.exports = async function handler(req, res) {
  const { text, voice = DEFAULT_VOICE } = req.query;
  if (!text) return res.status(400).end('missing text');
  try {
    const audio = await japaneseTTS(text, voice);
    res.setHeader('Content-Type', 'audio/mpeg');
    res.setHeader('Cache-Control', 'no-store');
    res.end(audio);
  } catch (e) {
    console.error('TTS-JA error:', e.message);
    res.status(500).end('TTS error');
  }
};
```

- [ ] **Step 2: Verify the file exists**

```bash
ls C:\Users\0deco\Downloads\espagnol-ai\api\
```

Expected: `tts-ja.js` appears alongside `tts-fr.js`, `tts-zh.js`, etc.

- [ ] **Step 3: Commit**

```bash
cd C:\Users\0deco\Downloads\espagnol-ai
git add api/tts-ja.js
git commit -m "feat: add Japanese TTS Vercel proxy"
```

---

### Task 3: Add LANGUAGES.ja + UI_STRINGS.ja to index.html

**Files:**
- Modify: `C:\Users\0deco\Downloads\espagnol-ai\index.html`

This task adds two blocks: the base LANGUAGES.ja entry (in the LANGUAGES literal object) and the full UI_STRINGS.ja entry.

- [ ] **Step 1: Add LANGUAGES.ja entry**

Locate this exact block (lines ~1399–1408):
```js
  zh: {
    defaultVoice: 'zh-CN-XiaoxiaoNeural',
    srLang: 'zh-CN',
    map: { lonMin: 70, lonMax: 145, latMax: 55, latMin: 18, w: 360, h: 296 },
    voices: null,
    meta:   null,
    system: null,
    diff:   null,
  },
};
```

Replace with:
```js
  zh: {
    defaultVoice: 'zh-CN-XiaoxiaoNeural',
    srLang: 'zh-CN',
    map: { lonMin: 70, lonMax: 145, latMax: 55, latMin: 18, w: 360, h: 296 },
    voices: null,
    meta:   null,
    system: null,
    diff:   null,
  },
  ja: {
    defaultVoice: 'ja-JP-NanamiNeural',
    srLang: 'ja-JP',
    map: { lonMin: 128, lonMax: 148, latMax: 46, latMin: 30, w: 360, h: 296 },
    voices: null,
    meta:   null,
    system: null,
    diff:   null,
  },
};
```

- [ ] **Step 2: Add UI_STRINGS.ja entry**

Locate this exact closing (lines ~1755–1759):
```js
「[セッション開始]」を受け取ったら、一文で紹介自己は考官，然后立即就材料提出第一个问题。`,
  },
};

function ui(key) { return UI_STRINGS[currentLang]?.[key] ?? UI_STRINGS.es[key]; }
```

Wait — the actual closing is:
```js
如果收到"[课程开始]"，请用一句话介绍自己是考官，然后立即就材料提出第一个问题。`,
  },
};

function ui(key) { return UI_STRINGS[currentLang]?.[key] ?? UI_STRINGS.es[key]; }
```

Replace the `};` + blank line + `function ui` with:

```js
如果收到"[课程开始]"，请用一句话介绍自己是考官，然后立即就材料提出第一个问题。`,
  },
  ja: {
    inactive:           '休止中',
    live:               'ライブ',
    speaking:           '話中',
    pressToTalk:        '押して話す',
    chatMode:           'チャット',
    settingsTitle:      '設定',
    settingsClose:      '閉じる ✕',
    settingsSubtitle:   '選択',
    settingsSubItalic:  '声',
    worldLabel:         '日本語の世界',
    durationLabel:      '時間',
    durationMin:        '分',
    dur1:               '1分',
    dur15:              '15',
    dur30:              '30分',
    examLabel:          '試験モード',
    examDesc:           'AIが文書を読んで試験します',
    chatLabel:          'チャットモード',
    chatDesc:           '静かに練習 — マイク不要',
    saveBtn:            '保存',
    chatPlaceholder:    '日本語で書いてください…',
    chatTopbar:         'チャットモード',
    uploadDoc:          '文書をアップロード',
    sessionSummary:     'セッションまとめ',
    analyzing:          '分析中…',
    newTitle:           '新しいタイトル',
    summaryPrompt:      'このセッションを3点でまとめてください：\n❌ 修正した間違い（正しい形付き）\n✅ うまくいったこと\n📚 使用した重要な語彙',
    summarySystem:      'あなたは教育アシスタントです。この日本語練習セッションを分析し、日本語で回答してください。簡潔、励まし、具体的に。',
    summaryError:       'まとめを利用できません。',
    silencePrompt:      '[沈黙]',
    sessionStart:       '[セッション開始]',
    fallbackGreeting:   'こんにちは！今日は何について話しましょうか？',
    examSystemFn: (content) =>
      `あなたは厳しいが温かい日本語の先生であり、試験官です。
学生は以下の学習資料をアップロードしました：
----
${content}
----
このセッションでのあなたの役割：
- この資料について日本語で質問する（具体的で段階的な質問）
- 回答を評価する：正しければ確認、間違いは自然に修正する
- 日本語の文法や語彙の間違いがあれば、修正して正しい形を繰り返させる
- 資料を体系的に進める（一度に一つの概念）
- 各ターン最大1〜2文
絶対に「さようなら」と言ったりセッションを終了しないこと——学生がいつ終わるかを決める。
「[沈黙]」を受け取ったら、最後の質問を別の言い方で言い直してください。
「[セッション開始]」を受け取ったら、一文で試験官として自己紹介し、すぐに資料についての最初の質問をしてください。`,
  },
};

function ui(key) { return UI_STRINGS[currentLang]?.[key] ?? UI_STRINGS.es[key]; }
```

- [ ] **Step 3: Commit**

```bash
cd C:\Users\0deco\Downloads\espagnol-ai
git add index.html
git commit -m "feat: add LANGUAGES.ja base + UI_STRINGS.ja"
```

---

### Task 4: Add LANGUAGES.ja.meta to index.html

**Files:**
- Modify: `C:\Users\0deco\Downloads\espagnol-ai\index.html`

CRITICAL: every voice meta MUST use `levels: [...]` (array of 3 strings) and `facts: [...]` (array of 3 strings). Using a singular `level`/`fact` causes a TypeError in `getVoiceLevelTitle()` that freezes the voice selector ring.

- [ ] **Step 1: Locate the insertion anchor**

Find this exact line (after the zh meta block closes):
```js
};

LANGUAGES.en.system = `You are an enthusiastic and friendly English conversation teacher.
```

- [ ] **Step 2: Insert LANGUAGES.ja.meta before LANGUAGES.en.system**

Insert between `};` and `LANGUAGES.en.system`:

```js
};

LANGUAGES.ja.meta = {
  'ja-JP-NanamiNeural': {
    levels: ['東京の初訪者', '下町の探索者', '江戸っ子'],
    facts: [
      '東京は人口約3700万人の世界最大の都市圏。地下鉄は300駅以上を擁し、年間40億回以上の乗車を誇る。',
      '浅草の浅草寺は628年創建の東京最古の寺で、毎年3000万人以上の参拝者が訪れる。',
      '東京のミシュラン星付きレストランの数は世界一。鮮度へのこだわりが世界トップの美食都市を作り上げた。',
    ],
  },
  'ja-JP-KeitaNeural': {
    levels: ['大阪の旅人', '道頓堀の常連', '天下の台所の主人'],
    facts: [
      '大阪は「天下の台所」と呼ばれ、江戸時代には日本全国の食料と物資の集積地として栄えた商業の中心地だった。',
      '大阪のたこ焼きは1930年代に生まれ、今も屋台や専門店で市民の日常食として愛されている。',
      '大阪城は1583年に豊臣秀吉によって築かれた。現在の天守閣は1931年の再建だが、一部の石垣は当時のままだ。',
    ],
  },
  'ja-JP-AoiNeural': {
    levels: ['雪国の旅人', '北海道の探検家', '開拓精神の継承者'],
    facts: [
      '札幌の雪まつりは毎年2月に開催され、200万人以上が訪れる世界最大規模の雪と氷の祭典だ。',
      '北海道の面積は日本の約22%を占めるが、人口は全国の約4%に過ぎず、広大な自然が広がる。',
      'サッポロビールは1876年創業の日本最古のビール醸造所のひとつで、明治の開拓とともに発展した。',
    ],
  },
  'ja-JP-DaichiNeural': {
    levels: ['仙台の旅人', '東北の探索者', '伊達者'],
    facts: [
      '仙台は「杜の都」と呼ばれ、伊達政宗が整備した並木道と豊かな緑が特徴の城下町だ。',
      '仙台七夕まつりは400年以上の歴史を持つ東北三大祭りのひとつ。8月に豪華な飾りが街を彩る。',
      '仙台の牛タン料理は戦後にアメリカ軍が牛を持ち込んだことをきっかけに発展した独自の食文化だ。',
    ],
  },
  'ja-JP-ShioriNeural': {
    levels: ['広島の旅人', '平和の門番', '平和の大使'],
    facts: [
      '広島平和記念公園の原爆ドームは1945年の原爆投下を生き延びた建物で、世界遺産に登録されている。',
      '広島風お好み焼きはキャベツ・そば・卵を重ねて焼くスタイルで、大阪とは明確に異なる地域食文化だ。',
      '宮島の厳島神社の鳥居は満潮時に海に浮かんで見え、平安時代から日本三景のひとつとして讃えられている。',
    ],
  },
  'ja-JP-NaokiNeural': {
    levels: ['博多の旅人', '屋台の常連', '九州の守り人'],
    facts: [
      '福岡の屋台文化は博多を代表する風物詩で、毎晩200軒以上の屋台が並びラーメンやもつ鍋を提供する。',
      '博多ラーメンは豚骨スープと細麺で知られ、九州を代表するスタイルとして全国に広まった。',
      '福岡は日本と大陸の玄関口として発展した歴史を持ち、現在も東京・大阪に次ぐスタートアップ都市として注目される。',
    ],
  },
};

LANGUAGES.en.system = `You are an enthusiastic and friendly English conversation teacher.
```

- [ ] **Step 3: Commit**

```bash
cd C:\Users\0deco\Downloads\espagnol-ai
git add index.html
git commit -m "feat: add LANGUAGES.ja.meta (6 voices, levels+facts arrays)"
```

---

### Task 5: Add LANGUAGES.ja.system + LANGUAGES.ja.diff to index.html

**Files:**
- Modify: `C:\Users\0deco\Downloads\espagnol-ai\index.html`

- [ ] **Step 1: Locate the insertion anchor**

Find this exact block (the end of the zh diff block, before `getLangVoices`):
```js
LANGUAGES.zh.diff = [
  '\n学生水平：A1–A2。使用简单短句，说话清晰。温和纠错，鼓励努力。',
  '\n学生水平：B1。使用自然语速和常用成语，在对话中流畅纠错。',
  '\n学生水平：B2+。像母语者一样说话——使用口语、幽默和你所在地区的文化典故。不要简化。',
];

function getLangVoices() { return LANGUAGES[currentLang].voices || LANGUAGES.es.voices; }
```

- [ ] **Step 2: Insert LANGUAGES.ja.system + diff between zh.diff and getLangVoices**

Replace the block above with:

```js
LANGUAGES.zh.diff = [
  '\n学生水平：A1–A2。使用简单短句，说话清晰。温和纠错，鼓励努力。',
  '\n学生水平：B1。使用自然语速和常用成语，在对话中流畅纠错。',
  '\n学生水平：B2+。像母语者一样说话——使用口语、幽默和你所在地区的文化典故。不要简化。',
];

LANGUAGES.ja.system = `あなたは熱心で親しみやすい日本語の先生です。
学生は日本語を練習しています。自然な日本語で答えてください：
- 学生のレベルに合った明確な文
- 温かく励ます口調
- 自然な表現と適切な慣用句
- 関連する文化的背景
- 詰まったら言い換える——恥ずかしい思いをさせない

学生の回答に基づいて難易度を調整してください。返答は80字以内に。`;

LANGUAGES.ja.diff = [
  '\n学生レベル：A1–A2。シンプルで短い文を使う。はっきり話す。優しく修正し、努力を褒める。',
  '\n学生レベル：B1。自然なペースと一般的な表現を使う。会話の中でスムーズに間違いを修正する。',
  '\n学生レベル：B2+。母語話者のように話す——口語、ユーモア、地域の文化的参照を使う。簡略化しない。',
];

function getLangVoices() { return LANGUAGES[currentLang].voices || LANGUAGES.es.voices; }
```

- [ ] **Step 3: Commit**

```bash
cd C:\Users\0deco\Downloads\espagnol-ai
git add index.html
git commit -m "feat: add LANGUAGES.ja.system + diff"
```

---

### Task 6: Add LANGUAGES.ja.voices to index.html

**Files:**
- Modify: `C:\Users\0deco\Downloads\espagnol-ai\index.html`

Map formula (for reference — do not run, just for verification):
- x = (lon − 128) / 20 × 360
- y = (46 − lat) / 16 × 296

- [ ] **Step 1: Locate the insertion anchor**

Find this exact closing block (end of zh.voices, before hexToRgb):
```js
LANGUAGES.zh.voices = [
  { id:'zh-CN-XiaoxiaoNeural',  name:'小晓', city:'北京', country:'中国', trait:'精准', color:'#C03050', x:223, y:121 },
  { id:'zh-CN-YunxiNeural',     name:'云希', city:'上海', country:'中国', trait:'温暖', color:'#3070B0', x:247, y:190 },
  { id:'zh-CN-XiaohanNeural',   name:'小涵', city:'成都', country:'中国', trait:'自然', color:'#D08020', x:164, y:195 },
  { id:'zh-CN-YunyeNeural',     name:'云野', city:'广州', country:'中国', trait:'轻松', color:'#3A8060', x:208, y:255 },
  { id:'zh-CN-YunfengNeural',   name:'云枫', city:'昆明', country:'中国', trait:'清晰', color:'#7050A0', x:157, y:240 },
  { id:'zh-TW-HsiaoChenNeural', name:'晓臻', city:'台北', country:'台湾', trait:'优雅', color:'#2090A0', x:247, y:240 },
];

function hexToRgb(hex) {
```

- [ ] **Step 2: Insert ja.voices after zh.voices closing `];`**

Replace the block above with:

```js
LANGUAGES.zh.voices = [
  { id:'zh-CN-XiaoxiaoNeural',  name:'小晓', city:'北京', country:'中国', trait:'精准', color:'#C03050', x:223, y:121 },
  { id:'zh-CN-YunxiNeural',     name:'云希', city:'上海', country:'中国', trait:'温暖', color:'#3070B0', x:247, y:190 },
  { id:'zh-CN-XiaohanNeural',   name:'小涵', city:'成都', country:'中国', trait:'自然', color:'#D08020', x:164, y:195 },
  { id:'zh-CN-YunyeNeural',     name:'云野', city:'广州', country:'中国', trait:'轻松', color:'#3A8060', x:208, y:255 },
  { id:'zh-CN-YunfengNeural',   name:'云枫', city:'昆明', country:'中国', trait:'清晰', color:'#7050A0', x:157, y:240 },
  { id:'zh-TW-HsiaoChenNeural', name:'晓臻', city:'台北', country:'台湾', trait:'优雅', color:'#2090A0', x:247, y:240 },
];

LANGUAGES.ja.voices = [
  { id:'ja-JP-NanamiNeural',  name:'七海', city:'東京', country:'日本', trait:'丁寧',   color:'#C03060', x:211, y:191 },
  { id:'ja-JP-KeitaNeural',   name:'圭太', city:'大阪', country:'日本', trait:'元気',   color:'#D06020', x:135, y:209 },
  { id:'ja-JP-AoiNeural',     name:'葵',   city:'札幌', country:'日本', trait:'爽やか', color:'#2050A0', x:241, y:54  },
  { id:'ja-JP-DaichiNeural',  name:'大智', city:'仙台', country:'日本', trait:'温かい', color:'#305040', x:232, y:143 },
  { id:'ja-JP-ShioriNeural',  name:'詩織', city:'広島', country:'日本', trait:'穏やか', color:'#702080', x:81,  y:215 },
  { id:'ja-JP-NaokiNeural',   name:'直紀', city:'福岡', country:'日本', trait:'明るい', color:'#208080', x:43,  y:230 },
];

function hexToRgb(hex) {
```

- [ ] **Step 3: Commit**

```bash
cd C:\Users\0deco\Downloads\espagnol-ai
git add index.html
git commit -m "feat: add LANGUAGES.ja.voices (6 dots, Japan map)"
```

---

### Task 7: Wire speakChunk + whisperLang for Japanese

**Files:**
- Modify: `C:\Users\0deco\Downloads\espagnol-ai\index.html`

- [ ] **Step 1: Update whisperLang ternary**

Locate this exact line (~line 3477):
```js
      const whisperLang = currentLang === 'pt' ? 'pt' : currentLang === 'ar' ? 'ar' : currentLang === 'ru' ? 'ru' : currentLang === 'en' ? 'en' : currentLang === 'fr' ? 'fr' : currentLang === 'zh' ? 'zh' : 'es';
```

Replace with:
```js
      const whisperLang = currentLang === 'pt' ? 'pt' : currentLang === 'ar' ? 'ar' : currentLang === 'ru' ? 'ru' : currentLang === 'en' ? 'en' : currentLang === 'fr' ? 'fr' : currentLang === 'zh' ? 'zh' : currentLang === 'ja' ? 'ja' : 'es';
```

- [ ] **Step 2: Add ja branch in speakChunk**

Locate this exact block (~lines 3561–3565):
```js
  } else if (currentLang === 'zh') {
    const r = await fetch(`/api/tts-zh?text=${encodeURIComponent(text)}&voice=${encodeURIComponent(voice)}`);
    if (!r.ok) throw new Error(`TTS ${r.status}`);
    blob = await r.blob();
  } else {
```

Replace with:
```js
  } else if (currentLang === 'zh') {
    const r = await fetch(`/api/tts-zh?text=${encodeURIComponent(text)}&voice=${encodeURIComponent(voice)}`);
    if (!r.ok) throw new Error(`TTS ${r.status}`);
    blob = await r.blob();
  } else if (currentLang === 'ja') {
    const r = await fetch(`/api/tts-ja?text=${encodeURIComponent(text)}&voice=${encodeURIComponent(voice)}`);
    if (!r.ok) throw new Error(`TTS ${r.status}`);
    blob = await r.blob();
  } else {
```

- [ ] **Step 3: Commit**

```bash
cd C:\Users\0deco\Downloads\espagnol-ai
git add index.html
git commit -m "feat: wire speakChunk + whisperLang for Japanese"
```

---

### Task 8: Onboarding button + lang chip + langNames + CSS glyph

**Files:**
- Modify: `C:\Users\0deco\Downloads\espagnol-ai\index.html`

This task adds all the UI surfaces where Japanese must appear.

- [ ] **Step 1: Add CSS glyph for .ja**

Locate this exact block (~line 957):
```css
    .onboard-lang-glyph.zh {
      background: radial-gradient(circle at 35% 35%, #FFD060, #D83030, #8B0000);
      box-shadow: 0 0 18px rgba(216,48,48,0.25);
    }
```

Replace with:
```css
    .onboard-lang-glyph.zh {
      background: radial-gradient(circle at 35% 35%, #FFD060, #D83030, #8B0000);
      box-shadow: 0 0 18px rgba(216,48,48,0.25);
    }
    .onboard-lang-glyph.ja {
      background: radial-gradient(circle at 35% 35%, #FFEEEE, #E02020, #880000);
      box-shadow: 0 0 18px rgba(224,32,32,0.25);
    }
```

- [ ] **Step 2: Add onboarding button for Japanese**

Locate this exact block (~lines 1091–1097):
```html
        <button class="onboard-lang-btn" data-lang="zh">
          <div class="onboard-lang-glyph zh">中</div>
          <div class="onboard-lang-info">
            <div class="onboard-lang-name">中文</div>
            <div class="onboard-lang-sub">2 国 · 6 声音</div>
          </div>
        </button>
      </div>
```

Replace with:
```html
        <button class="onboard-lang-btn" data-lang="zh">
          <div class="onboard-lang-glyph zh">中</div>
          <div class="onboard-lang-info">
            <div class="onboard-lang-name">中文</div>
            <div class="onboard-lang-sub">2 国 · 6 声音</div>
          </div>
        </button>
        <button class="onboard-lang-btn" data-lang="ja">
          <div class="onboard-lang-glyph ja">日</div>
          <div class="onboard-lang-info">
            <div class="onboard-lang-name">日本語</div>
            <div class="onboard-lang-sub">日本 · 6 声</div>
          </div>
        </button>
      </div>
```

- [ ] **Step 3: Add lang chip for Japanese**

Locate this exact line (~line 1225):
```html
          <button class="lang-chip" data-lang="zh">中文</button>
        </div>
```

Replace with:
```html
          <button class="lang-chip" data-lang="zh">中文</button>
          <button class="lang-chip" data-lang="ja">日本語</button>
        </div>
```

- [ ] **Step 4: Add Japanese to langNames**

Locate this exact line (~line 4234):
```js
const langNames = { es: 'Español', pt: 'Português', ar: 'العربية', ru: 'Русский', en: 'English', fr: 'Français', zh: '中文' };
```

Replace with:
```js
const langNames = { es: 'Español', pt: 'Português', ar: 'العربية', ru: 'Русский', en: 'English', fr: 'Français', zh: '中文', ja: '日本語' };
```

- [ ] **Step 5: Commit**

```bash
cd C:\Users\0deco\Downloads\espagnol-ai
git add index.html
git commit -m "feat: add Japanese onboarding button, lang chip, CSS glyph"
```

---

### Task 9: Bump SW to v64, commit espagnol-ai, push GitHub

**Files:**
- Modify: `C:\Users\0deco\Downloads\espagnol-ai\service-worker.js`

- [ ] **Step 1: Bump cache version**

In `service-worker.js` line 1, replace:
```js
const CACHE_NAME = 'espagnolai-v63';
```
with:
```js
const CACHE_NAME = 'espagnolai-v64';
```

- [ ] **Step 2: Final commit for espagnol-ai**

```bash
cd C:\Users\0deco\Downloads\espagnol-ai
git add service-worker.js
git commit -m "feat: add Japanese as 8th language (ja-JP, 6 voices, SW v64)"
```

- [ ] **Step 3: Push to GitHub (triggers Vercel auto-deploy)**

```bash
git push origin main
```

Expected: Vercel picks up the push and deploys within ~60 seconds. The Japanese language button, 6 voices, TTS proxy, and map will all be live.

- [ ] **Step 4: Smoke-test checklist (manual, in browser)**

Open the deployed URL and verify:
1. Onboarding screen shows 日本語 button with red Japanese glyph (distinct from Chinese gold glyph)
2. Selecting 日本語 starts a session; lang chip in settings reads 日本語
3. Press-to-talk triggers STT with `language: ja`
4. AI responds in Japanese
5. TTS plays Japanese audio (Nanami voice by default)
6. Voice selector shows 6 dots on Japan map — Tokyo, Osaka, Sapporo, Sendai, Hiroshima, Fukuoka — no overlap
7. Clicking each dot updates the ring, name, and city label correctly
8. Each voice's level titles (e.g. '東京の初訪者') display on the reward ring
9. Chat mode works (日本語で書いてください… placeholder)
10. Old service worker replaced (v64 in DevTools → Application → Cache Storage)

---

## Self-Review

### Spec coverage
- ✅ 6 ja-JP voices in voices.py — Task 1
- ✅ Vercel proxy /api/tts-ja.js — Task 2
- ✅ LANGUAGES.ja base + srLang + map — Task 3
- ✅ UI_STRINGS.ja (all keys, examSystemFn) — Task 3
- ✅ LANGUAGES.ja.meta (6 entries, levels[] + facts[]) — Task 4
- ✅ LANGUAGES.ja.system + diff[3] — Task 5
- ✅ LANGUAGES.ja.voices (6 entries with x,y) — Task 6
- ✅ speakChunk ja branch + whisperLang 'ja' — Task 7
- ✅ CSS .onboard-lang-glyph.ja — Task 8
- ✅ Onboarding button data-lang="ja" — Task 8
- ✅ lang-chip data-lang="ja" — Task 8
- ✅ langNames ja entry — Task 8
- ✅ SW v63 → v64 — Task 9
- ✅ GitHub push / Vercel deploy — Task 9

### Placeholder scan
None found. All code blocks are complete and literal.

### Type consistency
- `LANGUAGES.ja.meta` keys match `LANGUAGES.ja.voices` ids exactly (e.g. `'ja-JP-NanamiNeural'`)
- All `levels` and `facts` are arrays of 3 strings — matches what `getVoiceLevelTitle()` and `checkNewRewards()` expect
- `speakChunk` ja branch uses `/api/tts-ja` — matches the file created in Task 2
- `whisperLang` maps to `'ja'` — correct Whisper language code for Japanese
