# French Language Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add French as a 6th language to the espagnol-ai PWA, using 6 edge-tts voices (fr-FR, fr-BE, fr-CH, fr-CA) hosted on the existing `nolicae-nolicae-tts.hf.space` space — no new HF Space.

**Architecture:** Same pattern as English and Russian: add voice entries to `nolicae-tts/core/voices.py`, create a Vercel serverless proxy `/api/tts-fr.js`, then wire everything in `index.html` (LANGUAGES.fr, UI_STRINGS.fr, voices array with world-map coordinates, speakChunk routing). The meta format MUST use `levels` (array of 3) and `facts` (array of 3) — NOT the broken singular `level`/`fact` format used by RU/EN which caused a TypeError bug (since fixed in `getVoiceLevelTitle`).

**Tech Stack:** edge-tts (Microsoft Neural voices), HuggingFace Spaces Docker (nolicae-nolicae-tts.hf.space), Vercel serverless functions, vanilla JS PWA, service worker v62.

---

## Files

| File | Action |
|------|--------|
| `C:\Users\0deco\Downloads\nolicae-tts\core\voices.py` | Modify — append 6 French voice entries |
| `C:\Users\0deco\Downloads\espagnol-ai\api\tts-fr.js` | Create — Vercel proxy for French TTS |
| `C:\Users\0deco\Downloads\espagnol-ai\index.html` | Modify — 7 sections (LANGUAGES, UI_STRINGS, meta, system, diff, voices, speakChunk+whisper, onboarding+chip+langNames+CSS) |
| `C:\Users\0deco\Downloads\espagnol-ai\service-worker.js` | Modify — bump to v62 |

## Map projection for French

Bounds: `lonMin: -80, lonMax: 20, latMax: 56, latMin: 42, w: 360, h: 296`  
Formulas: `x = (lon + 80) / 100 * 360` · `y = (56 − lat) / 14 * 296`

| Voice | City | lon | lat | x | y |
|-------|------|-----|-----|---|---|
| fr-FR-DeniseNeural | Paris | 2.35 | 48.85 | 297 | 151 |
| fr-FR-HenriNeural | Marseille | 5.37 | 43.30 | 307 | 269 |
| fr-BE-CharlineNeural | Bruxelles | 4.35 | 50.85 | 304 | 109 |
| fr-CH-ArianeNeural | Genève | 6.15 | 46.23 | 310 | 207 |
| fr-CA-SylvieNeural | Montréal | −73.60 | 45.50 | 23 | 222 |
| fr-CA-AntoineNeural | Québec | −71.20 | 46.80 | 32 | 195 |

---

## Task 1 — Add French voices to nolicae-tts HF Space backend

**Files:**
- Modify: `C:\Users\0deco\Downloads\nolicae-tts\core\voices.py` (after English block, before closing `]`)

- [ ] **Step 1 — Open voices.py and find the insertion point**

  Locate the line:
  ```python
      {"languageCodes": ["en-NZ"], "name": "en-NZ-MollyNeural",   "ssmlGender": "FEMALE", "naturalSampleRateHertz": 24000, "backend": "edge"},
  ]
  ```

- [ ] **Step 2 — Insert French voices before the closing `]`**

  Replace that block with:
  ```python
      {"languageCodes": ["en-NZ"], "name": "en-NZ-MollyNeural",   "ssmlGender": "FEMALE", "naturalSampleRateHertz": 24000, "backend": "edge"},
      # French — 6 voices (fr-FR, fr-BE, fr-CH, fr-CA)
      {"languageCodes": ["fr-FR"], "name": "fr-FR-DeniseNeural",   "ssmlGender": "FEMALE", "naturalSampleRateHertz": 24000, "backend": "edge"},
      {"languageCodes": ["fr-FR"], "name": "fr-FR-HenriNeural",    "ssmlGender": "MALE",   "naturalSampleRateHertz": 24000, "backend": "edge"},
      {"languageCodes": ["fr-BE"], "name": "fr-BE-CharlineNeural", "ssmlGender": "FEMALE", "naturalSampleRateHertz": 24000, "backend": "edge"},
      {"languageCodes": ["fr-CH"], "name": "fr-CH-ArianeNeural",   "ssmlGender": "FEMALE", "naturalSampleRateHertz": 24000, "backend": "edge"},
      {"languageCodes": ["fr-CA"], "name": "fr-CA-SylvieNeural",   "ssmlGender": "FEMALE", "naturalSampleRateHertz": 24000, "backend": "edge"},
      {"languageCodes": ["fr-CA"], "name": "fr-CA-AntoineNeural",  "ssmlGender": "MALE",   "naturalSampleRateHertz": 24000, "backend": "edge"},
  ]
  ```

- [ ] **Step 3 — Quick smoke-test (count voices)**

  Run in the `nolicae-tts` directory:
  ```powershell
  Select-String -Path "core\voices.py" -Pattern "fr-FR|fr-BE|fr-CH|fr-CA" | Measure-Object | Select-Object -ExpandProperty Count
  ```
  Expected output: `6`

- [ ] **Step 4 — Commit and push to HF Space**

  ```powershell
  cd "C:\Users\0deco\Downloads\nolicae-tts"
  git add core/voices.py
  git commit -m "feat: add 6 French voices (fr-FR, fr-BE, fr-CH, fr-CA)"
  git push hf master:main
  ```
  Expected: `master -> main` push success message.

---

## Task 2 — Create Vercel proxy `/api/tts-fr.js`

**Files:**
- Create: `C:\Users\0deco\Downloads\espagnol-ai\api\tts-fr.js`

- [ ] **Step 1 — Create the file with this exact content**

  ```js
  // Vercel serverless function — nolicae-tts consolidated space (French voices)
  const TTS_BASE_URL = 'https://nolicae-nolicae-tts.hf.space';
  const DEFAULT_VOICE = 'fr-FR-DeniseNeural';
  const RETRY_DELAYS = [2000, 4000, 6000]; // retry up to 3x if space is waking

  async function frenchTTS(text, voice = DEFAULT_VOICE) {
    const body = JSON.stringify({
      input: { text },
      voice: { name: voice },
      audioConfig: { audioEncoding: 'MP3' }
    });

    let lastErr;
    for (let attempt = 0; attempt <= RETRY_DELAYS.length; attempt++) {
      if (attempt > 0) {
        await new Promise(r => setTimeout(r, RETRY_DELAYS[attempt - 1]));
      }
      try {
        const response = await fetch(`${TTS_BASE_URL}/v1/text:synthesize`, {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body,
        });
        if (!response.ok) {
          lastErr = new Error(`french-tts ${response.status}`);
          continue;
        }
        const data = await response.json();
        const audio = Buffer.from(data.audioContent, 'base64');
        if (audio.length < 100) {
          lastErr = new Error('audio too short, retrying');
          continue;
        }
        return audio;
      } catch (e) {
        lastErr = e;
      }
    }
    throw lastErr;
  }

  module.exports = async function handler(req, res) {
    const { text, voice = DEFAULT_VOICE } = req.query;
    if (!text) return res.status(400).end('missing text');
    try {
      const audio = await frenchTTS(text, voice);
      res.setHeader('Content-Type', 'audio/mpeg');
      res.setHeader('Cache-Control', 'no-store');
      res.end(audio);
    } catch (e) {
      console.error('TTS-FR error:', e.message);
      res.status(500).end('TTS error');
    }
  };
  ```

- [ ] **Step 2 — Verify file exists**

  ```powershell
  Test-Path "C:\Users\0deco\Downloads\espagnol-ai\api\tts-fr.js"
  ```
  Expected: `True`

---

## Task 3 — Add `LANGUAGES.fr` entry + `UI_STRINGS.fr` to index.html

**Files:**
- Modify: `C:\Users\0deco\Downloads\espagnol-ai\index.html` (two edits)

### 3a — Add `fr` to the LANGUAGES object (around line 1366)

- [ ] **Step 1 — Find and replace the LANGUAGES closing `};`**

  Locate (end of LANGUAGES object, after the `en:` block):
  ```js
    en: {
      defaultVoice: 'en-US-JennyNeural',
      srLang: 'en-US',
      map: { lonMin: -130, lonMax: 180, latMax: 60, latMin: -45, w: 360, h: 296 },
      voices: null,
      meta:   null,
      system: null,
      diff:   null,
    },
  };
  ```

  Replace with:
  ```js
    en: {
      defaultVoice: 'en-US-JennyNeural',
      srLang: 'en-US',
      map: { lonMin: -130, lonMax: 180, latMax: 60, latMin: -45, w: 360, h: 296 },
      voices: null,
      meta:   null,
      system: null,
      diff:   null,
    },
    fr: {
      defaultVoice: 'fr-FR-DeniseNeural',
      srLang: 'fr-FR',
      map: { lonMin: -80, lonMax: 20, latMax: 56, latMin: 42, w: 360, h: 296 },
      voices: null,
      meta:   null,
      system: null,
      diff:   null,
    },
  };
  ```

### 3b — Add `UI_STRINGS.fr` (after the closing `},` of `UI_STRINGS.en`)

- [ ] **Step 2 — Find the end of UI_STRINGS.en and insert fr block**

  Locate (the very last line before `};` that closes `UI_STRINGS`):
  ```js
    },
  };

  function ui(key) { return UI_STRINGS[currentLang]?.[key] ?? UI_STRINGS.es[key]; }
  ```

  Replace with:
  ```js
    },
    fr: {
      inactive:           'INACTIF',
      live:               'EN DIRECT',
      speaking:           'PARLE',
      pressToTalk:        'Appuyer pour parler',
      chatMode:           'CHAT',
      settingsTitle:      'Réglages',
      settingsClose:      'fermer ✕',
      settingsSubtitle:   'Choisissez une',
      settingsSubItalic:  'voix',
      worldLabel:         'Monde francophone',
      durationLabel:      'Durée',
      durationMin:        'min',
      dur1:               '1 MIN',
      dur15:              '15',
      dur30:              '30 MIN',
      examLabel:          'Mode Examen',
      examDesc:           'L\'IA lit un document et vous examine',
      chatLabel:          'Mode Chat',
      chatDesc:           'Pratiquez en silence — sans microphone',
      saveBtn:            'Enregistrer',
      chatPlaceholder:    'Écrivez en français…',
      chatTopbar:         'Mode Chat',
      uploadDoc:          'Charger un document',
      sessionSummary:     'Bilan de session',
      analyzing:          'Analyse en cours…',
      newTitle:           'Nouveau titre',
      summaryPrompt:      'Résume cette session en 3 points :\n❌ Erreurs corrigées (avec la bonne forme)\n✅ Ce qui s\'est bien passé\n📚 Vocabulaire clé utilisé',
      summarySystem:      'Tu es un assistant pédagogique. Analyse cette session de pratique du français et réponds en français. Sois bref, encourageant et concret.',
      summaryError:       'Résumé non disponible.',
      silencePrompt:      '[silence]',
      sessionStart:       '[début de session]',
      fallbackGreeting:   'Bonjour ! De quoi voulez-vous parler aujourd\'hui ?',
      examSystemFn: (content) =>
        `Tu es un professeur de français et un examinateur exigeant mais bienveillant.
  L'étudiant a chargé le matériel d'étude suivant :
  ----
  ${content}
  ----
  Ton rôle lors de cette session :
  - Interroger l'étudiant sur ce matériel en français, avec des questions concrètes et progressives
  - Évaluer ses réponses : valide ce qui est correct, corrige ce qui est faux de façon naturelle
  - S'il commet une erreur de français, corrige-la et demande-lui de répéter la bonne forme
  - Avance dans le matériel de façon structurée, un concept à la fois
  - Maximum 1-2 phrases par tour
  NE DIS JAMAIS au revoir ni ne clôtures la session — l'étudiant contrôle la fin.
  Si tu reçois "[silence]", reformule ta dernière question différemment.
  Si tu reçois "[début de session]", présente-toi comme examinateur en une phrase et pose immédiatement ta première question sur le matériel.`,
    },
  };

  function ui(key) { return UI_STRINGS[currentLang]?.[key] ?? UI_STRINGS.es[key]; }
  ```

---

## Task 4 — Add `LANGUAGES.fr.meta`

**Files:**
- Modify: `C:\Users\0deco\Downloads\espagnol-ai\index.html` (after LANGUAGES.en.meta block)

- [ ] **Step 1 — Find the insertion point**

  Locate this line (end of LANGUAGES.en.meta):
  ```js
  LANGUAGES.en.system = `You are an enthusiastic and friendly English conversation teacher.
  ```

- [ ] **Step 2 — Insert LANGUAGES.fr.meta immediately before that line**

  Insert the following block:
  ```js
  LANGUAGES.fr.meta = {
    'fr-FR-DeniseNeural': {
      levels: ['Parisien·ne de cœur', 'Habitant·e des Lumières', 'Gardien·ne du Français'],
      facts: [
        'Paris possède plus de 400 km de pistes cyclables et a doublé son réseau depuis 2020, devenant l\'une des capitales les plus cyclables d\'Europe.',
        'Le métro parisien compte 302 stations sur 220 km — l\'un des réseaux les plus denses du monde, dont certaines stations n\'ont pas changé depuis 1900.',
        'La Tour Eiffel mesure 15 cm de plus en été qu\'en hiver : le métal se dilate sous la chaleur, faisant « grandir » la tour.',
      ],
    },
    'fr-FR-HenriNeural': {
      levels: ['Marseillais d\'adoption', 'Fils du Vieux-Port', 'Gardien de la Méditerranée'],
      facts: [
        'Marseille est la plus ancienne ville de France, fondée par les Grecs sous le nom de Massalia vers 600 av. J.-C.',
        'La basilique Notre-Dame-de-la-Garde — la « Bonne Mère » — domine la ville à 149 m et sert de repère aux marins depuis des siècles.',
        'Le pastis, emblème de Marseille, se trouble dans l\'eau à cause de l\'anéthol : c\'est l\'effet « louche », signe qu\'il est authentique.',
      ],
    },
    'fr-BE-CharlineNeural': {
      levels: ['Bruxelloise de quartier', 'Citoyenne de l\'Europe', 'Ambassadrice du Plat Pays'],
      facts: [
        'Bruxelles abrite plus de 1 000 institutions européennes et internationales — c\'est l\'une des villes diplomatiques les plus actives au monde.',
        'La Grand-Place de Bruxelles, classée UNESCO, est entourée de 35 maisons de corporations médiévales reconstruites après le bombardement de 1695.',
        'Les Belges consomment en moyenne 6 kg de chocolat par an, et Bruxelles compte plus de 400 chocolatiers artisanaux dans ses ruelles.',
      ],
    },
    'fr-CH-ArianeNeural': {
      levels: ['Genevoise d\'esprit', 'Citoyenne du lac', 'Gardienne de la neutralité'],
      facts: [
        'Genève accueille le siège européen de l\'ONU, la Croix-Rouge internationale et plus de 200 organisations intergouvernementales.',
        'Le Jet d\'Eau du lac Léman s\'élève à 140 mètres de hauteur et pulse 500 litres d\'eau par seconde — visible à des kilomètres à la ronde.',
        'Jean-Jacques Rousseau, né à Genève en 1712, a posé les bases du contrat social et des droits naturels qui ont inspiré les révolutions moderne.',
      ],
    },
    'fr-CA-SylvieNeural': {
      levels: ['Montréalaise pure laine', 'Fille du Saint-Laurent', 'Gardienne du joual'],
      facts: [
        'Montréal est la deuxième plus grande ville francophone du monde après Paris, avec plus de 60 % de ses habitants parlant le français à la maison.',
        'Le RÉSO — la ville souterraine de Montréal — s\'étend sur 33 km et relie 80 bâtiments, permettant de survivre à l\'hiver sans mettre le manteau.',
        'Le Festival International de Jazz de Montréal est le plus grand au monde, avec près de 3 000 concerts par édition chaque été.',
      ],
    },
    'fr-CA-AntoineNeural': {
      levels: ['Québécois bon vivant', 'Fils de la Nouvelle-France', 'Gardien du fleurdelisé'],
      facts: [
        'Québec est la seule ville d\'Amérique du Nord à avoir conservé ses remparts médiévaux, inscrits au patrimoine mondial de l\'UNESCO depuis 1985.',
        'Le Carnaval de Québec est le plus grand carnaval d\'hiver du monde, célèbre pour ses sculptures de glace géantes et son bonhomme Carnaval.',
        'La ville a été fondée en 1608 par Samuel de Champlain — 12 ans avant que les Pèlerins ne débarquent à Plymouth Rock.',
      ],
    },
  };

  ```

---

## Task 5 — Add `LANGUAGES.fr.system` + `LANGUAGES.fr.diff`

**Files:**
- Modify: `C:\Users\0deco\Downloads\espagnol-ai\index.html` (after LANGUAGES.en.diff)

- [ ] **Step 1 — Find the insertion point**

  Locate:
  ```js
  LANGUAGES.en.diff = [
    '\nStudent level: A1–A2. Use simple, short sentences. Speak slowly and clearly. Correct gently and praise effort.',
    '\nStudent level: B1. Use natural pace and common idioms. Correct errors smoothly mid-conversation.',
    '\nStudent level: B2+. Speak as a native would — use colloquialisms, humour, cultural references from your region. Do not simplify.',
  ];

  function getLangVoices()
  ```

- [ ] **Step 2 — Insert French system + diff between the two**

  Replace with:
  ```js
  LANGUAGES.en.diff = [
    '\nStudent level: A1–A2. Use simple, short sentences. Speak slowly and clearly. Correct gently and praise effort.',
    '\nStudent level: B1. Use natural pace and common idioms. Correct errors smoothly mid-conversation.',
    '\nStudent level: B2+. Speak as a native would — use colloquialisms, humour, cultural references from your region. Do not simplify.',
  ];

  LANGUAGES.fr.system = `Tu es un professeur de français enthousiaste et bienveillant.
  Ton étudiant pratique son français. Réponds naturellement en français avec :
  - Des phrases claires adaptées à son niveau
  - Un ton chaleureux et encourageant
  - Des tournures naturelles, des expressions idiomatiques appropriées
  - Du contexte culturel quand c'est pertinent
  - Si l'étudiant bloque, reformule — ne le mets jamais mal à l'aise

  Adapte la difficulté en fonction de ses réponses. Garde tes réponses sous 80 mots.`;

  LANGUAGES.fr.diff = [
    '\nNiveau de l\'étudiant : A1–A2. Utilise des phrases simples et courtes. Parle clairement. Corrige avec douceur et encourage les efforts.',
    '\nNiveau de l\'étudiant : B1. Utilise un rythme naturel et des expressions courantes. Corrige les erreurs avec fluidité dans la conversation.',
    '\nNiveau de l\'étudiant : B2+. Parle comme un locuteur natif — utilise le familier, l\'humour, les références culturelles de ta région. Ne simplifie pas.',
  ];

  function getLangVoices()
  ```

---

## Task 6 — Add `LANGUAGES.fr.voices` with map coordinates

**Files:**
- Modify: `C:\Users\0deco\Downloads\espagnol-ai\index.html` (after LANGUAGES.en.voices, before `function hexToRgb`)

- [ ] **Step 1 — Find the insertion point**

  Locate:
  ```js
  LANGUAGES.en.voices = [
    { id:'en-US-JennyNeural',   name:'Jenny', ...
  ```
  ...and the closing line of that block:
  ```js
    { id:'en-NZ-MollyNeural',   name:'Molly',   city:'Auckland',     country:'New Zealand',  trait:'Cheerful',   color:'#40A0B0', x:354, y:273 },
  ];

  function hexToRgb(hex) {
  ```

- [ ] **Step 2 — Insert LANGUAGES.fr.voices before `function hexToRgb`**

  ```js
  LANGUAGES.fr.voices = [
    { id:'fr-FR-DeniseNeural',   name:'Denise',   city:'Paris',     country:'France',   trait:'Distinguée', color:'#C05070', x:297, y:151 },
    { id:'fr-FR-HenriNeural',    name:'Henri',    city:'Marseille', country:'France',   trait:'Chaleureux', color:'#E07030', x:307, y:269 },
    { id:'fr-BE-CharlineNeural', name:'Charline', city:'Bruxelles', country:'Belgique', trait:'Naturelle',  color:'#B09020', x:304, y:109 },
    { id:'fr-CH-ArianeNeural',   name:'Ariane',   city:'Genève',    country:'Suisse',   trait:'Précise',    color:'#50A050', x:310, y:207 },
    { id:'fr-CA-SylvieNeural',   name:'Sylvie',   city:'Montréal',  country:'Canada',   trait:'Énergique',  color:'#4870C0', x:23,  y:222 },
    { id:'fr-CA-AntoineNeural',  name:'Antoine',  city:'Québec',    country:'Canada',   trait:'Détendu',    color:'#8050A0', x:32,  y:195 },
  ];

  function hexToRgb(hex) {
  ```

---

## Task 7 — Wire speakChunk + whisperLang for French

**Files:**
- Modify: `C:\Users\0deco\Downloads\espagnol-ai\index.html` (two edits in the JS audio pipeline)

### 7a — whisperLang routing

- [ ] **Step 1 — Update the whisperLang ternary chain**

  Locate (line ~3203):
  ```js
    const whisperLang = currentLang === 'pt' ? 'pt' : currentLang === 'ar' ? 'ar' : currentLang === 'ru' ? 'ru' : currentLang === 'en' ? 'en' : 'es';
  ```

  Replace with:
  ```js
    const whisperLang = currentLang === 'pt' ? 'pt' : currentLang === 'ar' ? 'ar' : currentLang === 'ru' ? 'ru' : currentLang === 'en' ? 'en' : currentLang === 'fr' ? 'fr' : 'es';
  ```

### 7b — speakChunk routing

- [ ] **Step 2 — Add French branch in speakChunk**

  Locate:
  ```js
    } else if (currentLang === 'ru') {
      const r = await fetch(`/api/tts-ru?text=${encodeURIComponent(text)}&voice=${encodeURIComponent(voice)}`);
      if (!r.ok) throw new Error(`TTS ${r.status}`);
      blob = await r.blob();
    } else {
  ```

  Replace with:
  ```js
    } else if (currentLang === 'ru') {
      const r = await fetch(`/api/tts-ru?text=${encodeURIComponent(text)}&voice=${encodeURIComponent(voice)}`);
      if (!r.ok) throw new Error(`TTS ${r.status}`);
      blob = await r.blob();
    } else if (currentLang === 'fr') {
      const r = await fetch(`/api/tts-fr?text=${encodeURIComponent(text)}&voice=${encodeURIComponent(voice)}`);
      if (!r.ok) throw new Error(`TTS ${r.status}`);
      blob = await r.blob();
    } else {
  ```

---

## Task 8 — Onboarding button, lang chip, langNames, CSS glyph

**Files:**
- Modify: `C:\Users\0deco\Downloads\espagnol-ai\index.html` (4 edits)

### 8a — CSS glyph style

- [ ] **Step 1 — Add `.onboard-lang-glyph.fr` CSS**

  Locate:
  ```css
      .onboard-lang-glyph.en {
        background: radial-gradient(circle at 35% 35%, #B0C8E8, #4878B0, #1A3A60);
        box-shadow: 0 0 18px rgba(72,120,176,0.25);
      }
  ```

  Replace with:
  ```css
      .onboard-lang-glyph.en {
        background: radial-gradient(circle at 35% 35%, #B0C8E8, #4878B0, #1A3A60);
        box-shadow: 0 0 18px rgba(72,120,176,0.25);
      }
      .onboard-lang-glyph.fr {
        background: radial-gradient(circle at 35% 35%, #F0C0D0, #C05070, #702040);
        box-shadow: 0 0 18px rgba(192,80,112,0.25);
      }
  ```

### 8b — Onboarding button HTML

- [ ] **Step 2 — Add French onboarding button**

  Locate:
  ```html
          <button class="onboard-lang-btn" data-lang="en">
            <div class="onboard-lang-glyph en">E</div>
            <div class="onboard-lang-info">
              <div class="onboard-lang-name">English</div>
              <div class="onboard-lang-sub">10 countries · 10 voices</div>
            </div>
          </button>
        </div>
      </div>
  ```

  Replace with:
  ```html
          <button class="onboard-lang-btn" data-lang="en">
            <div class="onboard-lang-glyph en">E</div>
            <div class="onboard-lang-info">
              <div class="onboard-lang-name">English</div>
              <div class="onboard-lang-sub">10 countries · 10 voices</div>
            </div>
          </button>
          <button class="onboard-lang-btn" data-lang="fr">
            <div class="onboard-lang-glyph fr">F</div>
            <div class="onboard-lang-info">
              <div class="onboard-lang-name">Français</div>
              <div class="onboard-lang-sub">4 pays · 6 voix</div>
            </div>
          </button>
        </div>
      </div>
  ```

### 8c — Lang chip HTML

- [ ] **Step 3 — Add French lang chip in settings dropdown**

  Locate:
  ```html
            <button class="lang-chip" data-lang="en">English</button>
          </div>
  ```

  Replace with:
  ```html
            <button class="lang-chip" data-lang="en">English</button>
            <button class="lang-chip" data-lang="fr">Français</button>
          </div>
  ```

### 8d — langNames object

- [ ] **Step 4 — Add French to langNames**

  Locate:
  ```js
  const langNames = { es: 'Español', pt: 'Português', ar: 'العربية', ru: 'Русский', en: 'English' };
  ```

  Replace with:
  ```js
  const langNames = { es: 'Español', pt: 'Português', ar: 'العربية', ru: 'Русский', en: 'English', fr: 'Français' };
  ```

---

## Task 9 — Bump SW, commit espagnol-ai, push to GitHub

**Files:**
- Modify: `C:\Users\0deco\Downloads\espagnol-ai\service-worker.js`

- [ ] **Step 1 — Bump cache version to v62**

  Locate:
  ```js
  const CACHE_NAME = 'espagnolai-v61';
  ```

  Replace with:
  ```js
  const CACHE_NAME = 'espagnolai-v62';
  ```

- [ ] **Step 2 — Verify all 5 expected changes in espagnol-ai**

  ```powershell
  cd "C:\Users\0deco\Downloads\espagnol-ai"
  git diff --name-only
  ```
  Expected files changed: `api/tts-fr.js` (new), `index.html`, `service-worker.js`

- [ ] **Step 3 — Stage and commit**

  ```powershell
  cd "C:\Users\0deco\Downloads\espagnol-ai"
  git add api/tts-fr.js index.html service-worker.js
  git commit -m "feat: add French — 6 edge-tts voices (fr-FR/BE/CH/CA), world map selector, SW v62"
  ```

- [ ] **Step 4 — Push to GitHub (triggers Vercel deploy)**

  ```powershell
  git push origin main
  ```

- [ ] **Step 5 — Smoke-test after Vercel deploys (~1 min)**

  Open the app in browser → switch to Français → check:
  - [ ] Onboarding shows "Français" button with rose glyph
  - [ ] Settings map shows 6 dots across France/Belgium/Switzerland/Canada
  - [ ] Clicking Denise (Paris) highlights that dot and shows "Denise · Paris · France · Distinguée"
  - [ ] Clicking Antoine (Québec) moves the ring and shows correct info
  - [ ] Starting a session plays French TTS (Denise's voice)
  - [ ] Whisper transcribes French speech correctly

---

## Self-review

**Spec coverage:**
- ✅ French voices in HF Space backend (Task 1)
- ✅ Vercel proxy for French TTS (Task 2)
- ✅ LANGUAGES.fr with correct map bounds (Task 3)
- ✅ UI_STRINGS.fr — all 20 keys including examSystemFn (Task 3)
- ✅ LANGUAGES.fr.meta with proper `levels`/`facts` arrays — NOT singular `level`/`fact` (Task 4)
- ✅ LANGUAGES.fr.system + diff (Task 5)
- ✅ LANGUAGES.fr.voices with verified coordinates (Task 6)
- ✅ speakChunk + whisperLang routing (Task 7)
- ✅ Onboarding button + lang chip + langNames + CSS glyph (Task 8)
- ✅ SW bump + deploy (Task 9)

**Critical format note:** `LANGUAGES.fr.meta` uses `levels: [...]` (array) and `facts: [...]` (array), matching the Spanish format. Do NOT use `level: '...'` or `fact: '...'` (singular) — that format causes a TypeError in `getVoiceLevelTitle` that silently breaks the entire `selectVoice` function (the voice ring freezes and the name never updates).
