# Chinese (Mandarin) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add Chinese (Mandarin / 普通话) as a 7th language to the espagnol-ai PWA, using 6 edge-tts voices across China and Taiwan hosted on the existing `nolicae-nolicae-tts.hf.space` space — no new HF Space.

**Architecture:** Identical pattern to French (already shipped): append voice entries to `nolicae-tts/core/voices.py`, create Vercel serverless proxy `/api/tts-zh.js`, then wire `LANGUAGES.zh`, `UI_STRINGS.zh`, meta, system, diff, voices array, speakChunk routing, and onboarding UI in `index.html`. The meta format MUST use `levels: [...]` (array of 3) and `facts: [...]` (array of 3) — do NOT use singular `level`/`fact` (that format causes a TypeError in `getVoiceLevelTitle` that silently breaks the voice selector).

**Tech Stack:** edge-tts (Microsoft Neural voices), HuggingFace Spaces Docker (nolicae-nolicae-tts.hf.space), Vercel serverless functions, vanilla JS PWA, service worker v63.

---

## Files

| File | Action |
|------|--------|
| `C:\Users\0deco\Downloads\nolicae-tts\core\voices.py` | Modify — append 6 Chinese voice entries after the French block |
| `C:\Users\0deco\Downloads\espagnol-ai\api\tts-zh.js` | Create — Vercel proxy for Chinese TTS |
| `C:\Users\0deco\Downloads\espagnol-ai\index.html` | Modify — 7 sections (LANGUAGES, UI_STRINGS, meta, system+diff, voices, speakChunk+whisper, onboarding+chip+langNames+CSS) |
| `C:\Users\0deco\Downloads\espagnol-ai\service-worker.js` | Modify — bump to v63 |

## Map projection for Chinese-speaking world

Bounds: `lonMin: 70, lonMax: 145, latMax: 55, latMin: 18, w: 360, h: 296`  
Formulas: `x = round((lon − 70) / 75 * 360)` · `y = round((55 − lat) / 37 * 296)`

| Voice | Display name | City | Country | lon | lat | x | y |
|-------|-------------|------|---------|-----|-----|---|---|
| zh-CN-XiaoxiaoNeural | 小晓 | 北京 Beijing | 中国 | 116.4 | 39.9 | 223 | 121 |
| zh-CN-YunxiNeural | 云希 | 上海 Shanghai | 中国 | 121.5 | 31.2 | 247 | 190 |
| zh-CN-XiaohanNeural | 小涵 | 成都 Chengdu | 中国 | 104.1 | 30.6 | 164 | 195 |
| zh-CN-YunyeNeural | 云野 | 广州 Guangzhou | 中国 | 113.3 | 23.1 | 208 | 255 |
| zh-CN-YunfengNeural | 云枫 | 昆明 Kunming | 中国 | 102.7 | 25.0 | 157 | 240 |
| zh-TW-HsiaoChenNeural | 晓臻 | 台北 Taipei | 台湾 | 121.5 | 25.0 | 247 | 240 |

Min dot separation: Chengdu–Kunming 46 px · Guangzhou–Taipei 42 px (all ≥ 42 px ✅)

---

## Task 1 — Add Chinese voices to nolicae-tts HF Space backend

**Files:**
- Modify: `C:\Users\0deco\Downloads\nolicae-tts\core\voices.py` (after French block, before closing `]`)

- [ ] **Step 1 — Find the insertion point**

  Locate the last French voice line and the closing `]`:
  ```python
      {"languageCodes": ["fr-CA"], "name": "fr-CA-AntoineNeural",  "ssmlGender": "MALE",   "naturalSampleRateHertz": 24000, "backend": "edge"},
  ]
  ```

- [ ] **Step 2 — Insert Chinese voices before the closing `]`**

  Replace that block with:
  ```python
      {"languageCodes": ["fr-CA"], "name": "fr-CA-AntoineNeural",  "ssmlGender": "MALE",   "naturalSampleRateHertz": 24000, "backend": "edge"},
      # Chinese (Mandarin) — 6 voices (zh-CN, zh-TW)
      {"languageCodes": ["zh-CN"], "name": "zh-CN-XiaoxiaoNeural",  "ssmlGender": "FEMALE", "naturalSampleRateHertz": 24000, "backend": "edge"},
      {"languageCodes": ["zh-CN"], "name": "zh-CN-YunxiNeural",     "ssmlGender": "MALE",   "naturalSampleRateHertz": 24000, "backend": "edge"},
      {"languageCodes": ["zh-CN"], "name": "zh-CN-XiaohanNeural",   "ssmlGender": "FEMALE", "naturalSampleRateHertz": 24000, "backend": "edge"},
      {"languageCodes": ["zh-CN"], "name": "zh-CN-YunyeNeural",     "ssmlGender": "MALE",   "naturalSampleRateHertz": 24000, "backend": "edge"},
      {"languageCodes": ["zh-CN"], "name": "zh-CN-YunfengNeural",   "ssmlGender": "MALE",   "naturalSampleRateHertz": 24000, "backend": "edge"},
      {"languageCodes": ["zh-TW"], "name": "zh-TW-HsiaoChenNeural", "ssmlGender": "FEMALE", "naturalSampleRateHertz": 24000, "backend": "edge"},
  ]
  ```

- [ ] **Step 3 — Smoke-test (count voices)**

  ```powershell
  Select-String -Path "C:\Users\0deco\Downloads\nolicae-tts\core\voices.py" -Pattern "zh-CN|zh-TW" | Measure-Object | Select-Object -ExpandProperty Count
  ```
  Expected: `6`

- [ ] **Step 4 — Commit and push to HF Space**

  ```powershell
  cd "C:\Users\0deco\Downloads\nolicae-tts"
  git add core/voices.py
  git commit -m "feat: add 6 Chinese Mandarin voices (zh-CN, zh-TW)"
  git push hf master:main
  ```
  Expected: `master -> main` push success.

---

## Task 2 — Create Vercel proxy `/api/tts-zh.js`

**Files:**
- Create: `C:\Users\0deco\Downloads\espagnol-ai\api\tts-zh.js`

- [ ] **Step 1 — Create the file with this exact content**

  ```js
  // Vercel serverless function — nolicae-tts consolidated space (Chinese/Mandarin voices)
  const TTS_BASE_URL = 'https://nolicae-nolicae-tts.hf.space';
  const DEFAULT_VOICE = 'zh-CN-XiaoxiaoNeural';
  const RETRY_DELAYS = [2000, 4000, 6000]; // retry up to 3x if space is waking

  async function chineseTTS(text, voice = DEFAULT_VOICE) {
    const body = JSON.stringify({
      input: { text },
      voice: { name: voice },
      audioConfig: { audioEncoding: 'MP3' }
    });

    let lastErr;
    for (let attempt = 0; attempt <= RETRY_DELAYS.length; attempt++) {
      if (attempt > 0) {
        await new Promise(r => setTimeout(r, RETRY_DELAYS[attempt - 1]));
      }
      try {
        const response = await fetch(`${TTS_BASE_URL}/v1/text:synthesize`, {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body,
        });
        if (!response.ok) {
          lastErr = new Error(`chinese-tts ${response.status}`);
          continue;
        }
        const data = await response.json();
        const audio = Buffer.from(data.audioContent, 'base64');
        if (audio.length < 100) {
          lastErr = new Error('audio too short, retrying');
          continue;
        }
        return audio;
      } catch (e) {
        lastErr = e;
      }
    }
    throw lastErr;
  }

  module.exports = async function handler(req, res) {
    const { text, voice = DEFAULT_VOICE } = req.query;
    if (!text) return res.status(400).end('missing text');
    try {
      const audio = await chineseTTS(text, voice);
      res.setHeader('Content-Type', 'audio/mpeg');
      res.setHeader('Cache-Control', 'no-store');
      res.end(audio);
    } catch (e) {
      console.error('TTS-ZH error:', e.message);
      res.status(500).end('TTS error');
    }
  };
  ```

- [ ] **Step 2 — Verify file exists**

  ```powershell
  Test-Path "C:\Users\0deco\Downloads\espagnol-ai\api\tts-zh.js"
  ```
  Expected: `True`

---

## Task 3 — Add `LANGUAGES.zh` entry + `UI_STRINGS.zh` to index.html

**Files:**
- Modify: `C:\Users\0deco\Downloads\espagnol-ai\index.html` (two edits)

### 3a — Add `zh` to the LANGUAGES object

- [ ] **Step 1 — Find and replace the LANGUAGES closing `};`**

  Locate (end of LANGUAGES object, after the `fr:` block):
  ```js
    fr: {
      defaultVoice: 'fr-FR-DeniseNeural',
      srLang: 'fr-FR',
      map: { lonMin: -80, lonMax: 20, latMax: 56, latMin: 42, w: 360, h: 296 },
      voices: null,
      meta:   null,
      system: null,
      diff:   null,
    },
  };
  ```

  Replace with:
  ```js
    fr: {
      defaultVoice: 'fr-FR-DeniseNeural',
      srLang: 'fr-FR',
      map: { lonMin: -80, lonMax: 20, latMax: 56, latMin: 42, w: 360, h: 296 },
      voices: null,
      meta:   null,
      system: null,
      diff:   null,
    },
    zh: {
      defaultVoice: 'zh-CN-XiaoxiaoNeural',
      srLang: 'zh-CN',
      map: { lonMin: 70, lonMax: 145, latMax: 55, latMin: 18, w: 360, h: 296 },
      voices: null,
      meta:   null,
      system: null,
      diff:   null,
    },
  };
  ```

### 3b — Add `UI_STRINGS.zh` block

- [ ] **Step 2 — Find the end of UI_STRINGS and insert zh block**

  Locate (the very last part of UI_STRINGS, which ends with the French block):
  ```js
  Si tu reçois "[début de session]", présente-toi comme examinateur en une phrase et pose immédiatement ta première question sur le matériel.`,
    },
  };

  function ui(key) { return UI_STRINGS[currentLang]?.[key] ?? UI_STRINGS.es[key]; }
  ```

  Replace with:
  ```js
  Si tu reçois "[début de session]", présente-toi comme examinateur en une phrase et pose immédiatement ta première question sur le matériel.`,
    },
    zh: {
      inactive:           '休眠',
      live:               '直播中',
      speaking:           '说话中',
      pressToTalk:        '按下说话',
      chatMode:           '文字模式',
      settingsTitle:      '设置',
      settingsClose:      '关闭 ✕',
      settingsSubtitle:   '选择',
      settingsSubItalic:  '声音',
      worldLabel:         '华语世界',
      durationLabel:      '时长',
      durationMin:        '分',
      dur1:               '1分钟',
      dur15:              '15',
      dur30:              '30分钟',
      examLabel:          '考试模式',
      examDesc:           'AI读取文件并考察您',
      chatLabel:          '文字模式',
      chatDesc:           '无声练习 — 无需麦克风',
      saveBtn:            '保存',
      chatPlaceholder:    '用中文写作…',
      chatTopbar:         '文字模式',
      uploadDoc:          '上传文件',
      sessionSummary:     '课程总结',
      analyzing:          '分析中…',
      newTitle:           '新标题',
      summaryPrompt:      '用3点总结这次课程：\n❌ 已纠正的错误（附正确形式）\n✅ 做得好的地方\n📚 使用的关键词汇',
      summarySystem:      '你是一位教学助理。分析这次中文练习课并用中文回答。简洁、鼓励、具体。',
      summaryError:       '摘要不可用。',
      silencePrompt:      '[沉默]',
      sessionStart:       '[课程开始]',
      fallbackGreeting:   '你好！今天想聊什么？',
      examSystemFn: (content) =>
        `你是一位要求严格但温和的中文老师和考官。
学生上传了以下学习材料：
----
${content}
----
你在本次课程中的职责：
- 用中文就该材料向学生提问，问题具体且循序渐进
- 评估回答：正确则肯定，错误则自然地纠正
- 如果学生犯了中文语法或词汇错误，请纠正并让他重复正确的形式
- 有条理地推进材料，每次一个概念
- 每轮最多1-2句话
永远不要说再见或结束课程——学生控制结束时间。
如果收到"[沉默]"，请换一种方式重新提问。
如果收到"[课程开始]"，请用一句话介绍自己是考官，然后立即就材料提出第一个问题。`,
    },
  };

  function ui(key) { return UI_STRINGS[currentLang]?.[key] ?? UI_STRINGS.es[key]; }
  ```

---

## Task 4 — Add `LANGUAGES.zh.meta`

**Files:**
- Modify: `C:\Users\0deco\Downloads\espagnol-ai\index.html` (insert before LANGUAGES.en.system)

- [ ] **Step 1 — Find the insertion point**

  Locate this exact line (start of LANGUAGES.en.system):
  ```js
  LANGUAGES.en.system = `You are an enthusiastic and friendly English conversation teacher.
  ```

- [ ] **Step 2 — Insert LANGUAGES.zh.meta immediately before that line**

  ```js
  LANGUAGES.zh.meta = {
    'zh-CN-XiaoxiaoNeural': {
      levels: ['京城初客', '胡同漫步者', '普通话守护者'],
      facts: [
        '北京的故宫是世界上保存最完整的古代木质建筑群，由24位皇帝在明清两朝统治中国近600年。',
        '北京地铁线路总长超过800公里，日均客运量超过1200万人次，是全球最繁忙的地铁系统之一。',
        '北京烤鸭源于明朝，传统做法将整鸭悬挂于炉中慢烤，出炉时鸭皮焦脆金黄、香气扑鼻。',
      ],
    },
    'zh-CN-YunxiNeural': {
      levels: ['外滩游客', '弄堂行家', '申城守护者'],
      facts: [
        '上海是中国经济中心，常住人口超过2400万，GDP位居全国首位，是全球重要的金融中心之一。',
        '外滩的52栋历史建筑建于19至20世纪初，汇聚哥特、巴洛克等多种西方风格，被称为"万国建筑博览群"。',
        '上海话属于吴语，声调和词汇与普通话差异极大——"吃饭了吗"在上海话中说成"吃了伐"，外地人几乎听不懂。',
      ],
    },
    'zh-CN-XiaohanNeural': {
      levels: ['成都来客', '火锅老饕', '天府之国居民'],
      facts: [
        '成都的大熊猫繁育研究基地是全球规模最大的大熊猫繁育机构，距市中心仅10公里，年接待游客超过200万。',
        '成都火锅以麻辣著称，鲜汤底料中加入数十种香料，其配方有时被视为地方"非物质文化遗产"。',
        '成都茶馆文化超过2000年，至今老成都人仍习惯一杯盖碗茶、一把竹椅，在茶馆消磨整个下午。',
      ],
    },
    'zh-CN-YunyeNeural': {
      levels: ['广州新客', '珠江岸边人', '粤语美食家'],
      facts: [
        '广州早茶文化（饮茶）是粤式生活的精髓，每天清晨数十万市民聚集茶楼，品茶、吃点心、叙家常。',
        '广州是中国历史最悠久的对外贸易港口之一，两千年前已是海上丝绸之路的重要起点。',
        '粤语保留了大量古汉语音韵，许多唐诗用粤语朗读比普通话更接近原始发音，是语言学家的珍贵研究对象。',
      ],
    },
    'zh-CN-YunfengNeural': {
      levels: ['春城访客', '彩云追逐者', '云南文化使者'],
      facts: [
        '昆明被称为"春城"，因全年气温宜人，年均气温约15°C，冬无严寒、夏无酷暑，四季鲜花盛开。',
        '云南是中国少数民族最多的省份，共有25个世居少数民族，各族语言、服饰和节日习俗各具特色。',
        '昆明附近的石林是著名的喀斯特地貌奇观，270平方公里的石灰岩石柱如刀切斧劈，形成天然"石头森林"。',
      ],
    },
    'zh-TW-HsiaoChenNeural': {
      levels: ['台北旅人', '夜市常客', '台湾华语传承者'],
      facts: [
        '台北的夜市文化举世闻名，全市超过20处夜市汇聚数百种小吃，其中士林夜市年接待游客超过1000万人次。',
        '台北101在2004年至2010年间保持世界最高建筑纪录，竹节造型的外观灵感来自中国传统竹文化中坚韧不拔的意象。',
        '台湾华语与大陆普通话高度相通，但有独特词汇——如台湾说"捷运"，大陆说"地铁"；繁体字书写传统被视为文化遗产。',
      ],
    },
  };

  ```

---

## Task 5 — Add `LANGUAGES.zh.system` + `LANGUAGES.zh.diff`

**Files:**
- Modify: `C:\Users\0deco\Downloads\espagnol-ai\index.html` (after LANGUAGES.fr.diff, before function getLangVoices)

- [ ] **Step 1 — Find the insertion point**

  Locate:
  ```js
  LANGUAGES.fr.diff = [
    '\nNiveau de l\'étudiant : A1–A2. Utilise des phrases simples et courtes. Parle clairement. Corrige avec douceur et encourage les efforts.',
    '\nNiveau de l\'étudiant : B1. Utilise un rythme naturel et des expressions courantes. Corrige les erreurs avec fluidité dans la conversation.',
    '\nNiveau de l\'étudiant : B2+. Parle comme un locuteur natif — utilise le familier, l\'humour, les références culturelles de ta région. Ne simplifie pas.',
  ];

  function getLangVoices() { return LANGUAGES[currentLang].voices || LANGUAGES.es.voices; }
  ```

- [ ] **Step 2 — Insert Chinese system + diff between the two**

  Replace with:
  ```js
  LANGUAGES.fr.diff = [
    '\nNiveau de l\'étudiant : A1–A2. Utilise des phrases simples et courtes. Parle clairement. Corrige avec douceur et encourage les efforts.',
    '\nNiveau de l\'étudiant : B1. Utilise un rythme naturel et des expressions courantes. Corrige les erreurs avec fluidité dans la conversation.',
    '\nNiveau de l\'étudiant : B2+. Parle comme un locuteur natif — utilise le familier, l\'humour, les références culturelles de ta région. Ne simplifie pas.',
  ];

  LANGUAGES.zh.system = `你是一位热情友善的中文老师。
你的学生正在练习中文。用自然的中文回答，注意：
- 用清晰、适合其水平的句子
- 语气温暖、鼓励
- 使用自然的表达和合适的成语
- 适当融入文化背景
- 如果学生卡壳，换一种说法——绝不让他们感到尴尬

根据学生的回答调整难度。回答控制在80字以内。`;

  LANGUAGES.zh.diff = [
    '\n学生水平：A1–A2。使用简单短句，说话清晰。温和纠错，鼓励努力。',
    '\n学生水平：B1。使用自然语速和常用成语，在对话中流畅纠错。',
    '\n学生水平：B2+。像母语者一样说话——使用口语、幽默和你所在地区的文化典故。不要简化。',
  ];

  function getLangVoices() { return LANGUAGES[currentLang].voices || LANGUAGES.es.voices; }
  ```

---

## Task 6 — Add `LANGUAGES.zh.voices` with map coordinates

**Files:**
- Modify: `C:\Users\0deco\Downloads\espagnol-ai\index.html` (after LANGUAGES.fr.voices, before function hexToRgb)

- [ ] **Step 1 — Find the insertion point**

  Locate the closing lines of LANGUAGES.fr.voices:
  ```js
    { id:'fr-CA-AntoineNeural',  name:'Antoine',  city:'Québec',    country:'Canada',   trait:'Détendu',    color:'#8050A0', x:32,  y:195 },
  ];

  function hexToRgb(hex) {
  ```

- [ ] **Step 2 — Insert LANGUAGES.zh.voices before `function hexToRgb`**

  Replace with:
  ```js
    { id:'fr-CA-AntoineNeural',  name:'Antoine',  city:'Québec',    country:'Canada',   trait:'Détendu',    color:'#8050A0', x:32,  y:195 },
  ];

  LANGUAGES.zh.voices = [
    { id:'zh-CN-XiaoxiaoNeural',  name:'小晓', city:'北京', country:'中国', trait:'精准', color:'#C03050', x:223, y:121 },
    { id:'zh-CN-YunxiNeural',     name:'云希', city:'上海', country:'中国', trait:'温暖', color:'#3070B0', x:247, y:190 },
    { id:'zh-CN-XiaohanNeural',   name:'小涵', city:'成都', country:'中国', trait:'自然', color:'#D08020', x:164, y:195 },
    { id:'zh-CN-YunyeNeural',     name:'云野', city:'广州', country:'中国', trait:'轻松', color:'#3A8060', x:208, y:255 },
    { id:'zh-CN-YunfengNeural',   name:'云枫', city:'昆明', country:'中国', trait:'清晰', color:'#7050A0', x:157, y:240 },
    { id:'zh-TW-HsiaoChenNeural', name:'晓臻', city:'台北', country:'台湾', trait:'优雅', color:'#2090A0', x:247, y:240 },
  ];

  function hexToRgb(hex) {
  ```

---

## Task 7 — Wire speakChunk + whisperLang for Chinese

**Files:**
- Modify: `C:\Users\0deco\Downloads\espagnol-ai\index.html` (two edits)

### 7a — whisperLang routing

- [ ] **Step 1 — Update the whisperLang ternary chain**

  Locate:
  ```js
    const whisperLang = currentLang === 'pt' ? 'pt' : currentLang === 'ar' ? 'ar' : currentLang === 'ru' ? 'ru' : currentLang === 'en' ? 'en' : currentLang === 'fr' ? 'fr' : 'es';
  ```

  Replace with:
  ```js
    const whisperLang = currentLang === 'pt' ? 'pt' : currentLang === 'ar' ? 'ar' : currentLang === 'ru' ? 'ru' : currentLang === 'en' ? 'en' : currentLang === 'fr' ? 'fr' : currentLang === 'zh' ? 'zh' : 'es';
  ```

### 7b — speakChunk routing

- [ ] **Step 2 — Add Chinese branch in speakChunk**

  Locate:
  ```js
    } else if (currentLang === 'fr') {
      const r = await fetch(`/api/tts-fr?text=${encodeURIComponent(text)}&voice=${encodeURIComponent(voice)}`);
      if (!r.ok) throw new Error(`TTS ${r.status}`);
      blob = await r.blob();
    } else {
  ```

  Replace with:
  ```js
    } else if (currentLang === 'fr') {
      const r = await fetch(`/api/tts-fr?text=${encodeURIComponent(text)}&voice=${encodeURIComponent(voice)}`);
      if (!r.ok) throw new Error(`TTS ${r.status}`);
      blob = await r.blob();
    } else if (currentLang === 'zh') {
      const r = await fetch(`/api/tts-zh?text=${encodeURIComponent(text)}&voice=${encodeURIComponent(voice)}`);
      if (!r.ok) throw new Error(`TTS ${r.status}`);
      blob = await r.blob();
    } else {
  ```

---

## Task 8 — Onboarding button, lang chip, langNames, CSS glyph

**Files:**
- Modify: `C:\Users\0deco\Downloads\espagnol-ai\index.html` (4 edits)

### 8a — CSS glyph style

- [ ] **Step 1 — Add `.onboard-lang-glyph.zh` CSS**

  Locate:
  ```css
      .onboard-lang-glyph.fr {
        background: radial-gradient(circle at 35% 35%, #F0C0D0, #C05070, #702040);
        box-shadow: 0 0 18px rgba(192,80,112,0.25);
      }
  ```

  Replace with:
  ```css
      .onboard-lang-glyph.fr {
        background: radial-gradient(circle at 35% 35%, #F0C0D0, #C05070, #702040);
        box-shadow: 0 0 18px rgba(192,80,112,0.25);
      }
      .onboard-lang-glyph.zh {
        background: radial-gradient(circle at 35% 35%, #FFD060, #D83030, #8B0000);
        box-shadow: 0 0 18px rgba(216,48,48,0.25);
      }
  ```

### 8b — Onboarding button HTML

- [ ] **Step 2 — Add Chinese onboarding button**

  Locate:
  ```html
          <button class="onboard-lang-btn" data-lang="fr">
            <div class="onboard-lang-glyph fr">F</div>
            <div class="onboard-lang-info">
              <div class="onboard-lang-name">Français</div>
              <div class="onboard-lang-sub">4 pays · 6 voix</div>
            </div>
          </button>
        </div>
      </div>
  ```

  Replace with:
  ```html
          <button class="onboard-lang-btn" data-lang="fr">
            <div class="onboard-lang-glyph fr">F</div>
            <div class="onboard-lang-info">
              <div class="onboard-lang-name">Français</div>
              <div class="onboard-lang-sub">4 pays · 6 voix</div>
            </div>
          </button>
          <button class="onboard-lang-btn" data-lang="zh">
            <div class="onboard-lang-glyph zh">中</div>
            <div class="onboard-lang-info">
              <div class="onboard-lang-name">中文</div>
              <div class="onboard-lang-sub">2 国 · 6 声音</div>
            </div>
          </button>
        </div>
      </div>
  ```

### 8c — Lang chip HTML

- [ ] **Step 3 — Add Chinese lang chip in settings dropdown**

  Locate:
  ```html
            <button class="lang-chip" data-lang="fr">Français</button>
          </div>
  ```

  Replace with:
  ```html
            <button class="lang-chip" data-lang="fr">Français</button>
            <button class="lang-chip" data-lang="zh">中文</button>
          </div>
  ```

### 8d — langNames object

- [ ] **Step 4 — Add Chinese to langNames**

  Locate:
  ```js
  const langNames = { es: 'Español', pt: 'Português', ar: 'العربية', ru: 'Русский', en: 'English', fr: 'Français' };
  ```

  Replace with:
  ```js
  const langNames = { es: 'Español', pt: 'Português', ar: 'العربية', ru: 'Русский', en: 'English', fr: 'Français', zh: '中文' };
  ```

---

## Task 9 — Bump SW to v63, commit espagnol-ai, push GitHub

**Files:**
- Modify: `C:\Users\0deco\Downloads\espagnol-ai\service-worker.js`

- [ ] **Step 1 — Bump cache version to v63**

  Locate:
  ```js
  const CACHE_NAME = 'espagnolai-v62';
  ```

  Replace with:
  ```js
  const CACHE_NAME = 'espagnolai-v63';
  ```

- [ ] **Step 2 — Verify changed files**

  ```powershell
  cd "C:\Users\0deco\Downloads\espagnol-ai"
  git diff --name-only
  ```
  Expected: `api/tts-zh.js` (new), `index.html`, `service-worker.js`

- [ ] **Step 3 — Stage and commit**

  ```powershell
  cd "C:\Users\0deco\Downloads\espagnol-ai"
  git add api/tts-zh.js index.html service-worker.js
  git commit -m "feat: add Chinese — 6 Mandarin voices (zh-CN/TW), world map selector, SW v63"
  ```

- [ ] **Step 4 — Push to GitHub (triggers Vercel deploy)**

  ```powershell
  git push origin main
  ```

- [ ] **Step 5 — Smoke-test after Vercel deploys (~1 min)**

  Open the app in browser → switch to 中文 → check:
  - [ ] Onboarding shows "中文" button with red-gold glyph
  - [ ] Settings map shows 6 dots across China and Taiwan
  - [ ] Clicking 小晓 (Beijing) highlights that dot and shows "小晓 · 北京 · 中国 · 精准"
  - [ ] Clicking 晓臻 (Taipei) moves the ring and shows correct info
  - [ ] Starting a session plays Chinese TTS (Xiaoxiao's voice)
  - [ ] Whisper transcribes Mandarin speech correctly

---

## Self-review

**Spec coverage:**
- ✅ Chinese voices in HF Space backend (Task 1)
- ✅ Vercel proxy for Chinese TTS (Task 2)
- ✅ LANGUAGES.zh with correct map bounds for China + Taiwan (Task 3)
- ✅ UI_STRINGS.zh — all 20 keys including examSystemFn in Chinese (Task 3)
- ✅ LANGUAGES.zh.meta with proper `levels`/`facts` arrays — NOT singular `level`/`fact` (Task 4)
- ✅ LANGUAGES.zh.system + diff in Chinese (Task 5)
- ✅ LANGUAGES.zh.voices with verified map coordinates — min 42 px separation (Task 6)
- ✅ speakChunk + whisperLang routing (Task 7)
- ✅ Onboarding button + lang chip + langNames + CSS glyph (Task 8)
- ✅ SW bump to v63 + deploy (Task 9)

**Critical format note:** `LANGUAGES.zh.meta` MUST use `levels: [...]` (array) and `facts: [...]` (array). Singular `level`/`fact` causes a TypeError in `getVoiceLevelTitle` that silently stops `selectVoice()` mid-execution — the ring freezes and the name never updates. French was implemented correctly; copy that pattern exactly.

**Voice authenticity note:** All 6 voices are Mandarin (普通话) speakers — `zh-CN-XiaoxiaoNeural`, `zh-CN-YunxiNeural`, `zh-CN-XiaohanNeural`, `zh-CN-YunyeNeural`, `zh-CN-YunfengNeural`, `zh-TW-HsiaoChenNeural`. No Cantonese voices included; this avoids confusing learners with a different spoken language.
